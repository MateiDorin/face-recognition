import './Navigation.css'

const Navigation = () => {
    return (
        <div>
            <nav className="sign-out f3 link black underline pa3 pointer">Sign Out</nav>
        </div>
    );
};

export default Navigation;